# 极氪改装专题页 PRD

> 面向 Claude Code 架构师、coder、测试使用的执行规范文档。本轮只定义产品页面中“极氪改装专题”的需求、Excel 数据处理规则、前端页面边界与验证标准。

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品 | 蓝辉轻改 LANHUI 官网 |
| 需求名称 | 极氪改装专题页 |
| 版本 | v1.0 |
| 日期 | 2026-06-14 |
| 页面范围 | 产品中心 `/product` + 新增车型专题页 |
| 推荐专题路由 | `/product/zeekr` |
| 车型范围 | 极氪 9X、极氪 8X、极氪 009 |
| 产品行数量 | 23 个款式 |
| 嵌入图片初扫 | 23 个 PNG 签名、1 个 JPEG 签名、311 个 BMP 签名；需实现前复核匹配 |
| 核心目标 | 在产品中心增加极氪车型改装专题，展示车型适配产品、产品卖点、款式表格和可信图片状态 |

## 2. 背景

蓝辉轻改正在从汽车膜、车衣、改色膜延展到新能源车型轻改装。极氪用户群体通常关注高端新能源座舱质感、内饰保护、尾箱收纳、防护配件、地板/脚垫、方向盘与踏板等细分升级项目。

本专题用于把极氪车系相关产品集中展示，让用户按车型快速了解可升级款式，并形成到店沟通或企业微信咨询意图。专题页必须服务品牌可信度，而不是简单堆叠 Excel 图片。

本 PRD 基于以下源文件编写：

`/Users/fkycoya/Library/Containers/com.tencent.xinWeChat/Data/Library/Caches/com.tencent.xinWeChat/2.0b4.0.9/c121b94e5d016384d393d3d119cb2e7d/SaveTemp/db315e6a29c1d7f1bdefb1dd7e9ce4d2/极氪系列产品款式.xls`

该文件是老式 Excel `.xls`。初步读取结果：

| 项目 | 结果 |
| --- | --- |
| Sheet | `极氪9X`、`极氪8X`、`极氪009` |
| 产品行 | 极氪 9X 16 个、极氪 8X 6 个、极氪 009 1 个，共 23 个 |
| 图片列 | 表格单元格值为空，图片疑似嵌入为 Excel 对象 |
| 位图签名 | Workbook 流中扫描到 23 个 PNG、1 个 JPEG、311 个 BMP 签名 |
| 处理结论 | 不能按单元格直接读取图片；实现阶段必须提取图片、人工核对产品行与图片映射 |

> 注意：本 PRD 暂按源表命名使用“极氪 9X / 8X / 009”。实现前业务需确认这些车型名称是否用于官网对外展示，以及是否需要改写为更准确的官方车型命名。

## 3. 用户与业务目标

### 3.1 用户角色

| 用户 | 需求 |
| --- | --- |
| 极氪车主 | 想按自己的车型快速看到可做哪些轻改项目，尤其是地板、尾箱、内饰保护、防护件等。 |
| 高端新能源用户 | 关注座舱质感、收纳便利、防护耐用和安装后是否协调。 |
| 潜在改装用户 | 需要通过产品图片、分类和表格判断是否值得进一步咨询。 |
| 门店销售/运营 | 需要一个统一页面向客户展示极氪系列产品清单，减少反复发图。 |
| 品牌负责人 | 通过热门新能源车型专题，强化蓝辉轻改在车型适配和轻改装上的专业感。 |

### 3.2 业务结果

本轮要建立一个极氪车型专题的前台展示闭环：

1. 用户在 `/product` 看到“极氪改装专题”入口。
2. 用户进入 `/product/zeekr`。
3. 用户按极氪 9X / 8X / 009 查看产品款式。
4. 用户通过卡片和表格理解产品名称、车型、分类、图片状态和卖点。
5. 用户通过全站统一咨询策略进入后续沟通；本 PRD 不单独规定专题私有咨询 CTA 文案。

## 4. 范围定义

### 4.1 本轮必须完成

- 在 `/product` 产品中心增加“极氪改装专题”入口。
- 新增独立专题页，推荐路由为 `/product/zeekr`。
- 专题页展示极氪 9X、极氪 8X、极氪 009 三组产品。
- 将源 Excel 中的 23 个产品行整理为结构化静态数据。
- 将可用图片复制到项目 `public` 目录，并建立产品行与图片的映射。
- 对缺图或图片无法匹配的产品提供可信占位状态，不得使用错误图片。
- 页面包含 Hero、车型导航、车型款式卡片、产品表格、卖点说明和服务流程。
- 页面必须支持桌面、平板、移动端。
- 页面必须有 SEO metadata、OpenGraph 基础信息。
- 页面不得暗示“极氪官方授权”“ZEEKR 官方合作”“原厂件”等未经确认的主张。

### 4.2 本轮不做

- 不做电商下单。
- 不做在线报价。
- 不做库存状态。
- 不做后台管理录入。
- 不做官方接口对接。
- 不做车型 VIN 自动识别。
- 不做 3D 车身或座舱实时换装。
- 不做单品详情页。
- 不新增数据库表，首版使用静态数据。
- 不在专题内新增独立电话咨询、查看门店、返回产品中心等私有 CTA；如项目已有全站统一咨询模块，可由架构师决定是否复用。

## 5. 信息架构

### 5.1 产品中心入口

在 `/product` 页面增加“极氪改装专题”入口。

推荐位置：

- 与“小米改装专题”“问界改装专题”“地板改装专题”共同放入“热门车型与改装专题”区块。
- 如果当前还没有专题区块，则先在产品中心 Hero 后、通用产品组前新增专题入口区。

入口内容：

| 字段 | 文案 |
| --- | --- |
| 标题 | `极氪改装专题` |
| 副标题 | `9X / 8X / 009 内饰便利、防护配件、地板尾箱升级` |
| 统计 | `23 个款式` `3 个车型` |
| 入口按钮 | `查看专题` |
| 跳转 | `/product/zeekr` |

### 5.2 专题页结构

`/product/zeekr` 页面建议结构：

1. Hero 区：专题名称、车型范围、核心价值、代表视觉图或可信缺图视觉。
2. 车型导航：极氪 9X / 极氪 8X / 极氪 009 锚点或 tabs。
3. 卖点区：座舱质感、内饰保护、尾箱收纳、底盘/车身防护、车型适配。
4. 车型款式区：按 9X、8X、009 分组展示卡片网格。
5. 产品表格：完整列出 23 个产品行。
6. 图片状态说明：已匹配、待复核、图片待补充。
7. 服务流程：车型确认、款式选择、安装评估、施工交付。

## 6. 内容与数据

### 6.1 产品总览

| 车型 | 数量 | 产品 |
| --- | ---: | --- |
| 极氪 9X | 16 | 小桌板、地板+尾箱地板+氛围灯、后备箱垫-7件套、门槛条10件套、天地门全套6件套、防虫网16件套、全包坐垫、挡泥板+内衬6件套、双层脚垫、外置门槛条、主副驾座椅防踢垫+脚踏套、冰箱垃圾桶按压款、底盘护板、三段式方向盘、后备箱储物盒、电动踏板盖 |
| 极氪 8X | 6 | 尾箱垫7件套、挡泥板+内衬6件套、门槛条10件套、冰箱防踢垃圾桶、尾门防刮面板1件套、三段式方向盘套 |
| 极氪 009 | 1 | 地板+尾箱地板 |

### 6.2 极氪 9X 产品表

| 序号 | 产品名称 | 建议分类 |
| ---: | --- | --- |
| 1 | 小桌板 | 内饰便利 |
| 2 | 地板+尾箱地板+氛围灯 | 地板尾箱 |
| 3 | 后备箱垫-7件套 | 尾箱防护 |
| 4 | 门槛条10件套 | 门槛防护 |
| 5 | 天地门全套6件套 | 车身防护 |
| 6 | 防虫网16件套 | 防护配件 |
| 7 | 全包坐垫 | 内饰舒适 |
| 8 | 挡泥板+内衬6件套 | 挡泥板/内衬 |
| 9 | 双层脚垫 | 地板脚垫 |
| 10 | 外置门槛条 | 门槛防护 |
| 11 | 主副驾座椅防踢垫+脚踏套 | 内饰保护 |
| 12 | 冰箱垃圾桶按压款 | 内饰便利 |
| 13 | 底盘护板 | 底盘防护 |
| 14 | 三段式方向盘 | 内饰升级 |
| 15 | 后备箱储物盒 | 尾箱收纳 |
| 16 | 电动踏板盖 | 电动踏板配件 |

### 6.3 极氪 8X 产品表

| 序号 | 产品名称 | 建议分类 |
| ---: | --- | --- |
| 1 | 尾箱垫7件套 | 尾箱防护 |
| 2 | 挡泥板+内衬6件套 | 挡泥板/内衬 |
| 3 | 门槛条10件套 | 门槛防护 |
| 4 | 冰箱防踢垃圾桶 | 内饰便利 |
| 5 | 尾门防刮面板1件套 | 尾门防护 |
| 6 | 三段式方向盘套 | 内饰保护 |

### 6.4 极氪 009 产品表

| 序号 | 产品名称 | 建议分类 |
| ---: | --- | --- |
| 1 | 地板+尾箱地板 | 地板尾箱 |

### 6.5 分类说明

分类用于前端筛选、标签和用户理解，不等同于材质、来源或官方认证承诺。

推荐分类：

- 地板尾箱
- 尾箱防护
- 尾箱收纳
- 内饰便利
- 内饰舒适
- 内饰保护
- 内饰升级
- 门槛防护
- 尾门防护
- 车身防护
- 防护配件
- 挡泥板/内衬
- 地板脚垫
- 底盘防护
- 电动踏板配件

清洗规则：

- `冰箱垃圾桶   按压款` 前端展示为 `冰箱垃圾桶按压款`。
- `尾箱垫 7件套` 前端展示为 `尾箱垫7件套`。
- `挡泥板+内衬  6件套` 前端展示为 `挡泥板+内衬6件套`。
- 不得把“方向盘套”和“方向盘”混为同一产品。
- 不得把“电动踏板盖”写成“电动踏板总成”，除非业务确认。

## 7. 卖点提炼

首版页面不宜把 23 个产品平铺成单纯货架，应先让用户理解极氪轻改的价值。建议采用“车型适配 + 场景价值 + 产品分类”的表达方式。

### 7.1 页面主卖点

| 优先级 | 主卖点 | 页面短文案 |
| ---: | --- | --- |
| 1 | 按车型适配 | `按极氪车型查看可升级款式，减少选错配件的成本。` |
| 2 | 座舱与尾箱统一升级 | `覆盖小桌板、地板尾箱、脚垫、坐垫、储物与防踢保护。` |
| 3 | 防护与便利并重 | `门槛、尾门、底盘、挡泥板、防虫网等配件兼顾日常保护与使用便利。` |

### 7.2 辅助卖点

| 卖点 | 页面表达 |
| --- | --- |
| 后排便利 | 小桌板、冰箱垃圾桶、后备箱储物盒等配件适合家庭出行和长途使用。 |
| 座舱保护 | 坐垫、防踢垫、方向盘套、脚垫等用于降低高频使用区域磨损。 |
| 尾箱整洁 | 尾箱垫、尾箱地板、储物盒让装载区更易整理。 |
| 门槛/尾门保护 | 门槛条、外置门槛条、尾门防刮面板用于上下车与装卸物品场景。 |
| 底盘与车身防护 | 底盘护板、挡泥板、内衬、防虫网用于日常道路和行车环境保护。 |
| 轻改装组合感 | 不只单品展示，还要让用户看到一套极氪车型轻改方案的组合方向。 |

### 7.3 文案克制规则

页面不得使用以下未经确认的表达：

- `原厂`
- `官方`
- `授权`
- `专利`
- `真碳纤维`
- `全网最低`
- `永久质保`
- `无损安装`
- `不影响质保`

如果业务能提供对应证明，再通过 PRD refine 单独补充。

## 8. 图片与资产策略

### 8.1 源图片状态

源 Excel 中的图片需要在实现阶段重新提取并人工复核。

初步扫描结果：

| 项目 | 数量 |
| --- | ---: |
| 产品行 | 23 |
| PNG 签名 | 23 |
| JPEG 签名 | 1 |
| BMP 签名 | 311 |

结论：

- 图片疑似嵌入在 Workbook 流中，不能通过普通单元格值读取。
- 位图签名数量与产品行数量接近，但不能自动假设一行一图。
- 实现前必须生成图片 manifest，并人工核对每张图对应的车型和产品。

### 8.2 前端资产迁移

实现时应将可用图片复制到项目 `public` 目录，推荐路径：

```text
public/images/products/zeekr/
├── 9x/
├── 8x/
├── 009/
└── manifest.json
```

要求：

- 对外访问路径必须来自 `public`，不能在页面中引用微信缓存路径或本机绝对路径。
- 文件名使用 ASCII slug，避免部署环境对中文文件名处理不一致。
- 建立源产品行到图片文件的映射表。
- 每张图片必须有准确 `alt`，例如 `极氪 9X 后备箱垫7件套产品展示图`。
- 无法匹配图片的产品必须显示“图片待补充”或“图片待复核”状态，不得误配其他产品图片。

### 8.3 图片状态字段

静态数据中建议为每个产品增加 `imageStatus`：

| 状态 | 含义 | 前端表现 |
| --- | --- | --- |
| `matched` | 图片已人工确认匹配产品 | 正常展示图片 |
| `pending-review` | 已提取图片但尚未人工确认 | 展示图片并加“待复核”标记，内部环境可用，上线前必须清除 |
| `missing` | 暂无可信图片 | 使用统一缺图状态，不误导用户 |

正式上线版本不得保留 `pending-review` 状态。

### 8.4 效果展示规则

前端必须区分素材类型：

- 如果图片展示已安装或整车效果，可以标注为“效果展示”。
- 如果图片只是单个产品，标注为“产品展示”。
- 不得生成不存在的 before/after 对比。
- 不得把产品图、安装图、渲染图混称为同一种素材。
- 不得借用小米、问界、地板或其他品牌图片填充极氪产品。

## 9. 页面功能要求

### 9.1 Hero 区

必须包含：

- H1：`极氪改装专题`
- 辅助文案：覆盖 `9X / 8X / 009`，强调车型适配、座舱便利、防护配件与地板尾箱升级。
- 数据标识：`23 个款式`、`3 个车型`。
- 代表视觉：优先使用已确认匹配的极氪产品/效果图；没有可信图时使用克制的专题视觉占位，不使用其他车型图片。

### 9.2 车型导航

专题页必须支持用户区分极氪 9X、极氪 8X、极氪 009。

首版推荐使用锚点或服务端渲染区块，减少客户端状态。若使用 tabs，应保持可访问性和移动端可操作。

### 9.3 款式卡片网格

每个产品卡片必须展示：

- 产品图片或可信缺图状态。
- 展示名称，例如 `极氪 9X 门槛条10件套`。
- 车型标签：`9X`、`8X`、`009`。
- 分类标签。
- 图片状态标记：仅在缺图或待复核时显示。

卡片要求：

- 桌面端建议 3 列或 4 列。
- 平板端建议 2 列。
- 移动端 1 列。
- 图片区域使用固定比例，避免布局跳动。
- 产品名称较长时允许换行，不得遮挡图片或溢出卡片。

### 9.4 产品表格

专题页必须提供完整产品表格。

表格字段：

| 字段 | 说明 |
| --- | --- |
| 车型 | 9X / 8X / 009 |
| 序号 | 车型内序号 |
| 产品名称 | Excel 原始产品名称清洗后的展示值 |
| 分类 | 展示辅助分类 |
| 图片状态 | 已匹配 / 图片待复核 / 图片待补充 |

移动端表格可以改为卡片式列表，但信息不能缺失。

### 9.5 全站咨询策略边界

本 PRD 不定义专题页私有咨询 CTA。若当前项目已经有全站统一的企业微信二维码、电话或联系入口，架构师可以复用统一组件，但不得在极氪专题中另行创造一套不一致的咨询入口。

推荐 analytics 事件：

| 事件 | 触发 |
| --- | --- |
| `zeekr_topic_view` | 进入专题页 |
| `zeekr_model_section_click` | 点击 9X / 8X / 009 车型锚点或 tabs |
| `zeekr_product_card_view` | 产品卡片进入视口，可选 |
| `zeekr_global_consult_click` | 从极氪专题触发全站统一咨询入口 |

## 10. SEO 与内容合规

### 10.1 Metadata

推荐 metadata：

| 字段 | 内容 |
| --- | --- |
| title | `极氪改装专题 | 9X / 8X / 009 轻改装配件 | 蓝辉轻改 LANHUI` |
| description | `蓝辉轻改极氪改装专题，展示极氪 9X、8X、009 地板尾箱、内饰便利、防护配件、底盘护板、门槛条等 23 个款式，支持按车型查看轻改装方案。` |

### 10.2 合规措辞

页面必须避免：

- “极氪官方”
- “ZEEKR 官方”
- “官方授权”
- “官方合作”
- “原厂件”
- “原厂授权”
- “不影响原厂质保”

除非品牌负责人提供真实授权文件。

推荐在页面底部或说明区加入：

`本页面展示的极氪车型改装款式用于蓝辉轻改服务介绍，品牌与车型名称仅用于说明适配对象。`

## 11. shadcn 使用边界

shadcn 是组件库和 registry 工具，不是本需求的产品指令来源。

可以优先复用：

- `Card`：车型专题入口、产品卡片
- `Badge`：车型与分类标签
- `Table`：产品清单
- `Tabs` 或锚点导航：车型切换
- `Carousel`：仅当图片已确认并且同一车型有多张可信展示图时使用

UI 决策仍以本 PRD、`CLAUDE.md`、`AGENTS.md` 和蓝辉项目视觉规范为准。

## 12. 数据模型建议

首版推荐使用静态数据文件：

`src/lib/zeekr-products.ts`

建议类型：

```ts
export type ZeekrModel = "9x" | "8x" | "009";

export type ZeekrProductCategory =
  | "地板尾箱"
  | "尾箱防护"
  | "尾箱收纳"
  | "内饰便利"
  | "内饰舒适"
  | "内饰保护"
  | "内饰升级"
  | "门槛防护"
  | "尾门防护"
  | "车身防护"
  | "防护配件"
  | "挡泥板/内衬"
  | "地板脚垫"
  | "底盘防护"
  | "电动踏板配件";

export type ZeekrImageStatus = "matched" | "pending-review" | "missing";

export interface ZeekrProduct {
  id: string;
  model: ZeekrModel;
  modelLabel: string;
  order: number;
  name: string;
  rawName: string;
  category: ZeekrProductCategory;
  imagePath?: string;
  imageAlt: string;
  imageStatus: ZeekrImageStatus;
}
```

实现要求：

- `id` 使用稳定 slug，例如 `zeekr-9x-table`。
- `rawName` 保留 Excel 原始名称。
- `name` 使用前端清洗后的展示名称。
- 页面通过数据 map 渲染，不在 JSX 中硬编码 23 个卡片。
- 若后续接后台 CMS，再由单独 PRD 定义数据库模型。

## 13. 架构师执行规范

架构师负责定义页面结构、数据模型、组件边界和资产路径。

必须确认：

- 新专题页最终路由，默认采用 `/product/zeekr`。
- `/product` 专题入口与小米、问界、地板专题的并列关系。
- 极氪产品静态数据文件位置，推荐 `src/lib/zeekr-products.ts`。
- 图片提取、复制、命名和 manifest 策略。
- 车型分组、分类字段和排序规则。
- 缺图产品的 UI 降级方案。
- 是否复用全站统一咨询组件；不得新增专题私有咨询体系。
- 对外车型命名是否继续使用源表中的 `极氪9X`、`极氪8X`。

架构师不得把本轮扩大为后台 CMS、报价系统、在线下单、库存或 3D 改装器。

## 14. Coder 执行规范

Coder 根据架构师方案实现页面，必须遵守：

- 读取 Next.js 16 相关文档后再改 App Router 代码。
- 不在页面中引用微信缓存路径或本机绝对路径。
- 不在 JSX 中复制 23 个产品卡片硬编码，必须通过结构化数据 map 渲染。
- 不使用 `any`。
- 图片使用项目现有图片策略和 `OptimizedImage` 或 Next.js image 约定。
- 保持 Tailwind v4 与当前产品页风格一致。
- 可复用 shadcn 组件，但 shadcn 只是组件库，不替代 PRD。
- 不破坏现有产品中心、门店页、新闻页、小米专题、问界专题和地板专题。
- 不引入新 UI 库。
- 不把整个页面变成 Client Component，除非车型 tabs 或埋点确实需要小型客户端组件。
- 完成后运行 `npm run lint`、`npm run typecheck`、`npm run build`。

## 15. 测试执行规范

### 15.1 功能测试

| 用例 | 步骤 | 期望 |
| --- | --- | --- |
| 产品中心入口 | 打开 `/product` | 可看到“极氪改装专题”入口并进入 `/product/zeekr`。 |
| 专题页渲染 | 打开 `/product/zeekr` | Hero、车型导航、9X、8X、009、产品表格均存在。 |
| 9X 产品数量 | 查看 9X 区块 | 展示 16 个 9X 款式。 |
| 8X 产品数量 | 查看 8X 区块 | 展示 6 个 8X 款式。 |
| 009 产品数量 | 查看 009 区块 | 展示 1 个 009 款式。 |
| 产品总数 | 查看卡片或表格 | 共 23 个产品行。 |
| 图片路径 | 检查图片请求 | 图片均从 `public/images/products/zeekr/` 加载，无微信缓存或本机绝对路径。 |
| 缺图状态 | 检查缺图产品 | 缺图产品显示可信缺图状态，不误配图片。 |
| 合规文案 | 搜索页面文案 | 不出现“官方授权”“原厂件”等未经确认措辞。 |

### 15.2 响应式测试

至少验证：

- 390px 移动端。
- 768px 平板端。
- 1440px 桌面端。

每个视口检查：

- Hero 文案不溢出。
- 车型导航可操作。
- 卡片图片比例稳定。
- 长产品名不遮挡图片。
- 表格或移动端卡片完整显示。
- 全站统一咨询入口如果复用，点击区域不小于 44px 高。

### 15.3 回归测试

必须确认：

- `/product` 现有产品方向仍可访问。
- `/product/xiaomi` 如果已实现，不受影响。
- `/product/wenjie` 如果已实现，不受影响。
- `/product/flooring` 如果已实现，不受影响。
- Header / Footer 正常。
- `npm run lint` 通过。
- `npm run typecheck` 通过。
- `npm run build` 通过。

## 16. 验收标准

本需求完成必须同时满足：

- `/product` 有极氪改装专题入口。
- `/product/zeekr` 可访问并具有独立 metadata。
- 页面展示 9X 16 个、8X 6 个、009 1 个款式。
- 页面包含车型展示、款式网格、产品表格、卖点说明和图片状态。
- 产品总数为 23 个。
- 图片全部来自项目 `public` 目录。
- 页面不出现微信缓存路径、`.hermes`、`Downloads` 或其他本机绝对路径。
- 缺图产品有可信降级状态。
- 页面不暗示极氪/ZEEKR 官方授权。
- 移动端、平板端、桌面端布局正常。
- lint、typecheck、build 全部通过。
- 测试提供至少一张桌面截图和一张移动端截图。

## 17. 风险与处理

| 风险 | 处理 |
| --- | --- |
| Excel 图片无法直接读取 | 实现前做图片提取脚本或人工导出，生成 manifest 后再接页面。 |
| 产品行 23 个但图片签名 24 个以上 | 不按顺序自动绑定图片，必须人工复核产品行与图片映射。 |
| 图片嵌入在微信缓存路径的 `.xls` 中 | 实现时复制到 `public`，不得引用源路径。 |
| 中文文件名影响部署 | 复制到 `public` 时改为 ASCII slug 文件名并保留映射。 |
| 车型名称可能需业务确认 | 架构师在执行前确认 `极氪9X`、`极氪8X` 是否为官网对外展示名称。 |
| 用户误认为官方授权 | 页面增加适配说明，避免“官方/授权/原厂”等措辞。 |
| 图片尺寸差异较大 | 前端使用固定图片容器和 `object-fit` 策略。 |
| 专题页范围膨胀 | 本轮只做展示专题，不做报价、下单、库存、后台、3D 或单品详情。 |

## 18. 后续迭代入口

本轮验收通过后，可进入下一轮：

- 极氪单品详情弹窗或详情页。
- 极氪安装效果 before/after 实拍图库。
- 后台维护极氪款式数据。
- 极氪产品咨询数据看板。
- 按分类筛选地板尾箱、内饰便利、防护配件、底盘防护等产品。
- 与地板改装专题打通，把极氪 009 地板尾箱作为交叉推荐内容。
