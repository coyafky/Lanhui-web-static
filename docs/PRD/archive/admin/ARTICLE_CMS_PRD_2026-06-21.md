# 蓝辉文章内容管理系统 PRD

> **上位文档**：[ADMIN_SYSTEM_PRD_2026-06-21.md](./ADMIN_SYSTEM_PRD_2026-06-21.md)  
> **系统范围**：`/admin/articles`、`/admin/articles/new`、`/admin/articles/[id]` 及关联 API/图片能力  
> **用户**：总部 admin、editor  
> **版本**：v0.1（需求澄清中）  
> **创建日期**：2026-06-21  
> **状态**：规划中，未授权编码

---

## 1. 系统目标

让总部运营人员能够完成文章从构思、编辑、预览、发布、撤回、重新发布到归档的完整生命周期，并支持封面图、正文配图、官网 SEO 和微信公众号内容复用。

系统必须保证：

- 已发布内容可控，误发后可以快速撤回。
- 撤回不丢失文章和历史信息。
- 草稿与曾经发布过的文章有明确区别。
- 图片失败、保存失败和会话过期时尽量保留用户输入。
- 每个发布状态变化都有权限校验和操作日志。

### 1.1 一期产品原则：极简发布

- 初稿字段越少越好，让运营人员可以快速开始写作。
- 发布步骤越短越好，不引入没有明确业务需要的审核流。
- 一期不为了“功能完整”增加复杂版本、多人协作或高级 SEO 表单。
- 只有会直接影响官网展示或内容识别的字段才设为发布必填。
- 高级能力后续按真实运营问题逐步增加。

---

## 2. 用户与权限初稿

| 能力 | admin | editor | 未登录 |
|---|---:|---:|---:|
| 查看文章列表 | ✅ | ✅ | ❌ |
| 新建和编辑草稿 | ✅ | ✅ | ❌ |
| 预览文章 | ✅ | ✅ | ❌ |
| 发布文章 | ✅ | 待确认 | ❌ |
| 撤回和重新发布 | ✅ | 待确认 | ❌ |
| 归档和恢复 | ✅ | 待确认 | ❌ |
| 永久删除 | 待确认 | ❌ | ❌ |

---

## 3. 文章状态机

### 3.1 状态定义

| 状态 | 含义 | 官网可见 | 是否可编辑 |
|---|---|---:|---:|
| `draft` 草稿 | 从未正式发布 | 否 | 是 |
| `published` 已发布 | 当前正式展示 | 是 | 是，保存规则待确认 |
| `withdrawn` 已撤回 | 曾发布，当前下线 | 否 | 是 |
| `archived` 已归档 | 不再维护的历史内容 | 否 | 默认只读 |

### 3.2 允许的状态转换

```text
draft ──publish──> published ──withdraw──> withdrawn
  │                   ▲                       │
  │                   └──── republish ────────┘
  │
  └──archive──> archived

withdrawn ──archive──> archived
archived ──restore──> draft
```

### 3.3 状态动作规则

| 动作 | 前置状态 | 目标状态 | 前置条件 | 官网影响 |
|---|---|---|---|---|
| 保存草稿 | draft | draft | 标题可暂不完整，规则待确认 | 无 |
| 发布 | draft | published | 必填字段、正文和发布校验全部通过 | 立即可见 |
| 撤回 | published | withdrawn | 必须填写撤回原因或确认，待确认 | 立即隐藏 |
| 重新发布 | withdrawn | published | 再次通过发布校验 | 恢复可见 |
| 归档 | draft/withdrawn | archived | 二次确认 | 后台归档区可查 |
| 恢复编辑 | archived | draft | 二次确认 | 无 |

### 3.4 禁止转换

- `published → draft`：禁止直接转换，必须先进入 `withdrawn`，保留“曾发布”事实。
- `archived → published`：禁止直接发布，必须先恢复为草稿并重新校验。
- 默认不允许永久删除已发布过的文章，最终删除策略待确认。

---

## 4. 页面地图

| 页面 | 主要任务 | 必须覆盖的状态 |
|---|---|---|
| `/admin/articles` | 搜索、筛选、查看状态、执行状态动作 | 加载、空、筛选无结果、失败、批量操作 |
| `/admin/articles/new` | 创建和保存草稿 | 未保存、保存中、保存成功、校验失败、离开提醒 |
| `/admin/articles/[id]` | 编辑、预览、发布、撤回、归档 | 4 种文章状态、冲突、会话过期 |
| 文章预览 | 查看官网最终效果 | 草稿/撤回文章的授权预览 |

---

## 5. 文章字段初稿

| 字段 | 草稿必填 | 发布必填 | 说明 |
|---|---:|---:|---|
| 标题 | 是 | 是 | 草稿最低识别字段 |
| slug | 否，系统自动生成 | 是且唯一 | 默认从标题生成，运营无需手填 |
| 摘要 | 否 | 否 | 可从正文自动截取，允许手工修改 |
| 正文 | 否 | 是 | 核心内容 |
| 分类 | 否 | 是 | 使用少量固定分类 |
| 标签 | 否 | 否 | 一期可隐藏或取消 |
| 封面图 | 否 | 待确认 | 是否作为发布必填仍需确认 |
| 正文配图 | 否 | 否 | 支持多图 |
| 作者 | 系统自动 | 是 | 不要求运营选择 |
| SEO 标题 | 不展示独立输入 | 系统生成 | 默认使用文章标题 |
| SEO 描述 | 不展示独立输入 | 系统生成 | 优先摘要，否则截取正文 |
| 发布时间 | 否 | 系统自动 | 重新发布规则待确认 |

### 5.1 一期推荐表单

运营人员默认只看到：

1. 标题。
2. 分类。
3. 正文。
4. 封面图。

摘要放入可选的“更多设置”；slug、作者、SEO 和发布时间由系统自动处理。标签一期默认不展示。

---

## 6. 编辑与保存流程（待细化）

必须定义：

- 新文章首次保存。
- 自动保存或手动保存策略。
- 编辑已发布文章时，修改是立即影响线上，还是先形成待发布修改稿。
- 离开页面时存在未保存内容的提醒。
- 多个总部员工同时编辑的冲突处理。
- 保存失败后本地内容保留与重试。
- 会话过期后的重新登录和内容恢复。

### 6.1 已发布文章修改策略

该决策会直接影响：

- 线上内容是否可能被未完成修改覆盖。
- 是否需要文章版本、修改稿或发布快照。
- 编辑页按钮与状态提示。
- 多人编辑冲突和回滚方式。
- 发布、撤回与重新发布的测试范围。

候选方案：

#### 方案 A：直接修改线上版本

- 编辑 `published` 文章后点击保存，官网立即更新。
- 实现简单，但误操作会直接影响线上内容。
- 不适合需要多次修改、预览或内部确认的文章。

#### 方案 B：线上版本 + 修改稿

- `published` 文章继续保持当前线上版本。
- 编辑时产生独立修改稿，不立即影响官网。
- 运营预览并点击“更新发布”后，修改稿替换线上版本。
- 放弃修改稿不会影响线上文章。
- 需要版本或修改稿数据结构，但发布安全性更高。

#### 方案 C：先撤回再编辑

- 已发布文章必须撤回为 `withdrawn` 后才能编辑。
- 规则简单且安全，但每次小改都会导致文章临时下线。
- 不利于纠错、更新数字或替换图片等日常维护。

基于“一期极简发布”原则，复杂修改稿和完整版本历史不默认进入一期；最终采用方案 A 还是简化后的安全保存方式，待下一步确认。

---

## 7. 图片与媒体（待细化）

- 封面图上传、预览、替换和删除。
- 正文图片上传、插入、排序、替代文本和删除引用。
- 图片格式、大小、尺寸、压缩和 WebP 转换。
- 上传失败、部分成功和重试。
- 删除文章或替换图片时的孤儿文件处理。
- 公众号复制/导出时的图片地址处理。

---

## 8. 微信公众号复用（待确认）

一期推荐 CMS 为网站内容源，提供公众号友好的复制或导出，不做自动发布和双向同步。

仍需确认：

- 网站首发还是公众号首发。
- 需要纯文本、Markdown、HTML 还是公众号富文本格式。
- 图片是否自动转入公众号素材库。

---

## 9. 异常与恢复清单

| 场景 | 预期处理 |
|---|---|
| slug 重复 | 阻止保存并定位到 slug 字段 |
| 图片上传失败 | 保留正文和其他字段，允许单独重试 |
| 保存时网络中断 | 保留编辑内容，显示重试入口 |
| 文章已被他人修改 | 阻止静默覆盖，展示冲突信息 |
| 会话过期 | 提示重新登录，尽量恢复未保存内容 |
| 发布校验失败 | 不改变状态，逐项展示缺失字段 |
| 撤回失败 | 文章保持 published，不显示虚假成功 |
| 重新发布失败 | 文章保持 withdrawn |

---

## 10. 测试范围初稿

- 四种状态的显示和筛选。
- 所有允许和禁止的状态转换。
- 撤回后官网立即不可见。
- 重新发布后官网恢复可见。
- 归档文章默认只读。
- 草稿、撤回、归档文章不能被公开 API 获取。
- 字段校验、slug 冲突、网络失败和重复提交。
- editor/admin 权限差异。
- 封面和正文图片的上传、替换、失败恢复。
- 桌面、平板和移动端编辑体验。

---

## 11. 开放问题

1. 编辑已发布文章时，保存是否立即更新线上内容。
2. editor 是否拥有发布、撤回、归档权限。
3. 草稿允许保存到什么最低完整程度。
4. 是否需要待审核状态。
5. 是否需要自动保存和文章版本历史。
6. 是否允许永久删除，以及删除哪些状态。
7. 编辑器类型与公众号复用方式。

---

## 12. 变更记录

| 日期 | 版本 | 变更 | 作者 |
|---|---|---|---|
| 2026-06-21 | v0.1 | 新建文章 CMS 子 PRD；确认草稿、发布、撤回、归档状态机 | Coya / Codex |
| 2026-06-21 | v0.2 | 确认一期采用极简发布原则；收敛默认表单字段并自动生成 slug、作者和 SEO 信息 | Coya / Codex |
