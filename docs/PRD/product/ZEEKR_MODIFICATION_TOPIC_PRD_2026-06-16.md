# 极氪改装专题页 PRD(v2.0)

> 面向 Claude Code 架构师、coder、测试使用的执行规范文档。本版本基于 v1.0(2026-06-14,已归档为 `.archive`)重写,核心增量是把"图片尺寸大小一致"从软建议升级为项目级硬性规格约束。

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品 | 蓝辉轻改 LANHUI 官网 |
| 需求名称 | 极氪改装专题页 |
| 版本 | v2.0 |
| 日期 | 2026-06-16 |
| 上一版本 | v1.0(2026-06-14,已归档为 `ZEEKR_MODIFICATION_TOPIC_PRD_2026-06-14.md.archive`) |
| 页面范围 | 产品中心 `/product` + 新增车型专题页 |
| 推荐专题路由 | `/product/zeekr` |
| 车型范围 | 极氪 9X、极氪 8X、极氪 009 |
| 产品行数量 | 23 个款式(基于源 Excel 表格) |
| 已就位图片 | 21 张 PNG(9X 14 张 + 8X 6 张 + 009 1 张),已位于 `public/images/products/ZEEKR/{极氪9X,极氪8X,Zeeker009}/`,实际像素 1448×1086 / 4:3 |
| **缺图产品行** | **2 个:9X 行 8「挡泥板+内衬6件套」、9X 行 9「双层脚垫」——源 Excel 有产品行但 Excel 流中未提取到图片,UI 必须降级** |
| **本版定位** | **项目内图片规格标准化的首个落地专题。本 PRD §8 规格表将作为其他专题(问界、小米、地板等)图片整改的参考依据,后续专题 PRD 应直接 cite 本节规格。** |
| 核心目标 | 在产品中心增加极氪车型改装专题,以统一图片规格(1448×1086 / 4:3 / ASCII 命名)展示车型适配产品,并把"图片规格标准化"作为可验收硬性约束固化。 |

## 2. 背景

蓝辉轻改正在从汽车膜、车衣、改色膜延展到新能源车型轻改装。极氪用户群体通常关注高端新能源座舱质感、内饰保护、尾箱收纳、防护配件、地板/脚垫、方向盘与踏板等细分升级项目。

本专题用于把极氪车系相关产品集中展示,让用户按车型快速了解可升级款式,并形成到店沟通意图。专题页必须服务品牌可信度,而不是简单堆叠 Excel 图片。

### 2.1 项目内图片规格现状(本版新增背景)

本版调研发现,项目内其他专题的产品图在像素和宽高比上**未做统一约束**,存在多源拼凑问题:

| 专题 | 抽样文件 | 实际像素 | 比例 | 状态 |
| --- | --- | ---: | ---: | --- |
| 问界 | `wenjie/preview.png` | 1100 × 1552 | 11:15.5(竖图) | 与产品图比例割裂 |
| 小米 | `xiaomi/su7/su7-01-front-bumper.png` | 2523 × 1661 | 1.52:1 | 比例失控 |
| 小米 | `xiaomi/su7/su7-02-pedals.png` | 781 × 490 | 1.59:1 | 比例失控 |
| 小米 | `xiaomi/su7/su7-03-seat-back-panel.png` | 654 × 691 | 1:1.06(近正方形) | 比例失控 |
| 小米 | `xiaomi/su7/su7-04-side-skirt.png` | 989 × 660 | 1.50:1 | 比例失控 |
| 小米 | `xiaomi/su7/su7-05-steering-wheel.png` | 800 × 634 | 1.26:1 | 比例失控 |
| 极氪 | `ZEEKR/极氪9X/小桌板.png` 等 21 张 | **1448 × 1086** | **4:3** | **✅ 完全统一(2 行缺图不影响规格一致性)** |

**结论**:极氪 21 张已就位图片在物理层面已经统一到 1448×1086 / 4:3,是项目内目前唯一"实际尺寸合理"的专题。本版 PRD 把这一现状升级为可强制校验的硬性规格,作为项目级图片标准化的首个落地。

### 2.2 源文件

本 PRD 基于以下源文件编写:

`/Users/fkycoya/Library/Containers/com.tencent.xinWeChat/Data/Library/Caches/com.tencent.xinWeChat/2.0b4.0.9/c121b94e5d016384d393d3d119cb2e7d/SaveTemp/db315e6a29c1d7f1bdefb1dd7e9ce4d2/极氪系列产品款式.xls`

该文件是老式 Excel `.xls`。读取结果:

| 项目 | 结果 |
| --- | --- |
| Sheet | `极氪9X`、`极氪8X`、`极氪009` |
| 产品行 | 极氪 9X 16 个、极氪 8X 6 个、极氪 009 1 个,共 23 个 |
| 图片列 | 表格单元格值为空,图片疑似嵌入为 Excel 对象 |
| 位图签名 | Workbook 流中扫描到 23 个 PNG、1 个 JPEG、311 个 BMP 签名 |
| 处理结论 | 不能按单元格直接读取图片;实现阶段必须提取图片、人工核对产品行与图片映射 |

> 注意:本 PRD 暂按源表命名使用"极氪 9X / 8X / 009"。实现前业务需确认这些车型名称是否用于官网对外展示,以及是否需要改写为更准确的官方车型命名。

## 3. 用户与业务目标

### 3.1 用户角色

| 用户 | 需求 |
| --- | --- |
| 极氪车主 | 想按自己的车型快速看到可做哪些轻改项目,尤其是地板、尾箱、内饰保护、防护件等。 |
| 高端新能源用户 | 关注座舱质感、收纳便利、防护耐用和安装后是否协调。 |
| 潜在改装用户 | 需要通过产品图片、分类和表格判断是否值得进一步了解。 |
| 门店销售/运营 | 需要一个统一页面向客户展示极氪系列产品清单,减少反复发图。 |
| 品牌负责人 | 通过热门新能源车型专题,强化蓝辉轻改在车型适配和轻改装上的专业感。 |

### 3.2 业务结果

本轮要建立一个极氪车型专题的前台展示闭环:

1. 用户在 `/product` 看到"极氪改装专题"入口。
2. 用户进入 `/product/zeekr`。
3. 用户按极氪 9X / 8X / 009 查看产品款式。
4. 用户通过卡片和表格理解产品名称、车型、分类、图片状态和卖点。

## 4. 范围定义

### 4.1 本轮必须完成

- 在 `/product` 产品中心增加"极氪改装专题"入口(参照 wenjie / xiaomi / flooring 现有 TopicBanner 模式)。
- 新增独立专题页,推荐路由为 `/product/zeekr`。
- 专题页展示极氪 9X、极氪 8X、极氪 009 三组产品。
- 将源 Excel 中的 23 个产品行整理为结构化静态数据 `src/lib/zeekr-products.ts`。
- 将 21 张已就位 PNG 从 `public/images/products/ZEEKR/{极氪9X,极氪8X,Zeeker009}/` 复制到 `public/images/products/zeekr/{9x,8x,009}/`,文件名改为 ASCII slug。
- 在数据层固化图片规格(像素、比例),TypeScript 类型层面强制 1448×1086 / 4:3。
- 接入构建期校验脚本 `scripts/verify-zeekr-images.mjs`,校验 21 张图全部满足规格;2 行缺图(9X 行 8/9)不阻断 build,但 UI 必须降级。
- 页面包含 Hero、车型导航、车型款式卡片、产品表格、卖点说明和服务流程。
- 页面必须支持桌面、平板、移动端。
- 页面必须有 SEO metadata、OpenGraph 基础信息。
- 页面不得暗示"极氪官方授权""ZEEKR 官方合作""原厂件"等未经确认的主张。

### 4.2 本轮不做

- 不做电商下单。
- 不做在线报价。
- 不做库存状态。
- 不做后台管理录入。
- 不做官方接口对接。
- 不做车型 VIN 自动识别。
- 不做 3D 车身或座舱实时换装。
- 不做单品详情页。
- 不新增数据库表,首版使用静态数据。
- **本轮校验脚本只校验 zeekr,不顺带扫描 wenjie/xiaomi。** 问界/小米图片规格整改作为后续迭代入口(见 §18)。

## 5. 信息架构

### 5.1 产品中心入口

在 `/product` 页面增加"极氪改装专题"入口。

推荐位置:

- 与"小米改装专题""问界改装专题""地板改装专题"共同放入"热门车型与改装专题"区块。
- 在 `<XiaomiTopicBanner />`、`<WenjieTopicBanner />`、`<FlooringTopicBanner />` 之后追加 `<ZeekrTopicBanner />`。

入口内容:

| 字段 | 文案 |
| --- | --- |
| 标题 | `极氪改装专题` |
| 副标题 | `9X / 8X / 009 内饰便利、防护配件、地板尾箱升级` |
| 统计 | `23 个款式` `3 个车型` |
| 入口按钮 | `查看专题` |
| 跳转 | `/product/zeekr` |

### 5.2 专题页结构

`/product/zeekr` 页面建议结构(沿用 v1.0):

1. Hero 区:专题名称、车型范围、核心价值、代表视觉图。
2. 车型导航:极氪 9X / 极氪 8X / 极氪 009 锚点。
3. 卖点区:座舱质感、内饰保护、尾箱收纳、底盘/车身防护、车型适配。
4. 车型款式区:按 9X、8X、009 分组展示卡片网格。
5. 产品表格:完整列出 23 个产品行。
6. 图片状态说明:已匹配、待复核、图片待补充。
7. 服务流程:车型确认、款式选择、安装评估、施工交付。
8. 合规说明。

## 6. 内容与数据

### 6.1 产品总览

| 车型 | 数量 | 分类覆盖 |
| ---: | ---: | --- |
| 极氪 9X | 16 | 内饰便利、地板尾箱、尾箱防护、门槛防护、车身防护、防护配件、内饰舒适、挡泥板/内衬、地板脚垫、底盘防护、内饰升级、尾箱收纳、电动踏板配件、内饰保护 |
| 极氪 8X | 6 | 尾箱防护、挡泥板/内衬、门槛防护、内饰便利、尾门防护、内饰保护 |
| 极氪 009 | 1 | 地板尾箱 |

### 6.2 极氪 9X 产品表

| 序号 | 产品名称 | 建议分类 |
| ---: | --- | --- |
| 1 | 小桌板 | 内饰便利 |
| 2 | 地板+尾箱地板+氛围灯 | 地板尾箱 |
| 3 | 后备箱垫-7件套 | 尾箱防护 |
| 4 | 门槛条10件套 | 门槛防护 |
| 5 | 天地门全套6件套 | 车身防护 |
| 6 | 防虫网16件套 | 防护配件 |
| 7 | 全包坐垫 | 内饰舒适 |
| 8 | 挡泥板+内衬6件套 | 挡泥板/内衬 |
| 9 | 双层脚垫 | 地板脚垫 |
| 10 | 外置门槛条 | 门槛防护 |
| 11 | 主副驾座椅防踢垫+脚踏套 | 内饰保护 |
| 12 | 冰箱垃圾桶按压款 | 内饰便利 |
| 13 | 底盘护板 | 底盘防护 |
| 14 | 三段式方向盘 | 内饰升级 |
| 15 | 后备箱储物盒 | 尾箱收纳 |
| 16 | 电动踏板盖 | 电动踏板配件 |

### 6.3 极氪 8X 产品表

| 序号 | 产品名称 | 建议分类 |
| ---: | --- | --- |
| 1 | 尾箱垫7件套 | 尾箱防护 |
| 2 | 挡泥板+内衬6件套 | 挡泥板/内衬 |
| 3 | 门槛条10件套 | 门槛防护 |
| 4 | 冰箱防踢垃圾桶 | 内饰便利 |
| 5 | 尾门防刮面板1件套 | 尾门防护 |
| 6 | 三段式方向盘套 | 内饰保护 |

### 6.4 极氪 009 产品表

| 序号 | 产品名称 | 建议分类 |
| ---: | --- | --- |
| 1 | 地板+尾箱地板 | 地板尾箱 |

### 6.5 分类说明

分类用于前端筛选、标签和用户理解,不等同于材质、来源或官方认证承诺。

推荐分类(共 15 类):

- 地板尾箱、尾箱防护、尾箱收纳
- 内饰便利、内饰舒适、内饰保护、内饰升级
- 门槛防护、尾门防护、车身防护
- 防护配件、挡泥板/内衬、地板脚垫
- 底盘防护、电动踏板配件

清洗规则:

- `冰箱垃圾桶   按压款` 前端展示为 `冰箱垃圾桶按压款`。
- `尾箱垫 7件套` 前端展示为 `尾箱垫7件套`。
- `挡泥板+内衬  6件套` 前端展示为 `挡泥板+内衬6件套`。
- 不得把"方向盘套"和"方向盘"混为同一产品。
- 不得把"电动踏板盖"写成"电动踏板总成",除非业务确认。

## 7. 卖点提炼

首版页面不宜把 23 个产品平铺成单纯货架,应先让用户理解极氪轻改的价值。建议采用"车型适配 + 场景价值 + 产品分类"的表达方式。

### 7.1 页面主卖点

| 优先级 | 主卖点 | 页面短文案 |
| ---: | --- | --- |
| 1 | 按车型适配 | `按极氪车型查看可升级款式,减少选错配件的成本。` |
| 2 | 座舱与尾箱统一升级 | `覆盖小桌板、地板尾箱、脚垫、坐垫、储物与防踢保护。` |
| 3 | 防护与便利并重 | `门槛、尾门、底盘、挡泥板、防虫网等配件兼顾日常保护与使用便利。` |

### 7.2 辅助卖点

| 卖点 | 页面表达 |
| --- | --- |
| 后排便利 | 小桌板、冰箱垃圾桶、后备箱储物盒等配件适合家庭出行和长途使用。 |
| 座舱保护 | 坐垫、防踢垫、方向盘套、脚垫等用于降低高频使用区域磨损。 |
| 尾箱整洁 | 尾箱垫、尾箱地板、储物盒让装载区更易整理。 |
| 门槛/尾门保护 | 门槛条、外置门槛条、尾门防刮面板用于上下车与装卸物品场景。 |
| 底盘与车身防护 | 底盘护板、挡泥板、内衬、防虫网用于日常道路和行车环境保护。 |
| 轻改装组合感 | 不只单品展示,还要让用户看到一套极氪车型轻改方案的组合方向。 |

### 7.3 文案克制规则

页面不得使用以下未经确认的表达:

- `原厂`、`官方`、`授权`、`专利`、`真碳纤维`
- `全网最低`、`永久质保`、`无损安装`、`不影响质保`

如果业务能提供对应证明,再通过 PRD refine 单独补充。

## 8. 图片与资产策略(本版核心章节)

### 8.0 经验教训:为什么本版要固化图片规格

回顾 §2.1 现状盘点,项目内问界/小米专题的产品图**比例/像素均无约束**,直接后果:

- 卡片容器内图片大小不一,长宽比混合,布局跳动;
- 同一专题内不同产品图的视觉重量失衡,影响品牌专业感;
- 后续若用 Next/Image 优化(响应式 `sizes`、自动 srcset),无法稳定生成;
- 命名混乱(中文 / 英文拼写不一致如 `Zeeker009/1-borad.png` 中的 `borad` 应为 `board`),部署到 Linux/Vercel 时存在编码风险;
- **产品行 vs 图片文件不一一对应**(v1.0 PRD 假设 23 行=23 图,本版实地盘点发现 9X 14 图 / 8X 6 图 / 009 1 图,9X 行 8「挡泥板+内衬6件套」、行 9「双层脚垫」无对应 PNG,1 个 9X 文件「后备箱储物」与 PRD 行 15「后备箱储物盒」命名差异)——**这是 v1.0 隐式假设导致的盲点,本版显式拆出 `matched` / `pending-review` / `missing` 三态对冲**。

**本版极氪吸取以上教训,把图片规格从"软建议"升级为"项目级硬性约束"**:在物理文件层、数据类型层、构建期校验层、UI 容器层四个层面同时约束,缺一不可。

### 8.1 图片现状盘点

| 项 | 值 |
| --- | --- |
| 已就位文件总数 | 21(9X 14 + 8X 6 + 009 1) |
| 产品行总数 | 23(9X 16 + 8X 6 + 009 1) |
| 缺图产品行 | 2(均为 9X:行 8 挡泥板+内衬6件套、行 9 双层脚垫) |
| 已就位文件像素 | **1448 × 1086**(已天然一致) |
| 宽高比 | **4:3** |
| 格式 | PNG |
| 来源目录(迁移前) | `public/images/products/ZEEKR/{极氪9X,极氪8X,Zeeker009}/` |
| 目标目录(迁移后) | `public/images/products/zeekr/{9x,8x,009}/` |
| 文件命名 | 中文(迁移前) → ASCII slug(迁移后) |
| 状态分布 | 20 行 `matched`(图片已就位 + 命名对应产品行已核对,= 9X 13 + 8X 6 + 009 1)/ 1 行 `pending-review`(9X 行 15「后备箱储物.png」与 PRD §6.2 行 15「后备箱储物盒」命名差异,需业务核对)/ 2 行 `missing`(9X 行 8「挡泥板+内衬6件套」、行 9「双层脚垫」) |

> **说明**:上一版本 PRD 假设"23 行=23 图",本版实地盘点发现 21 张图、2 行缺图,1 个 9X 文件(`后备箱储物.png`)与 PRD §6.2 行 15「后备箱储物盒」是否同一产品需业务确认。本版显式拆出 `matched` / `pending-review` / `missing` 三态,避免"装作 23=23"的虚假完备。

### 8.2 物理规格硬性约束(项目级标准)

| 项 | 规格 | 验证方式 |
| --- | --- | --- |
| 像素 | 1448 × 1086 px(±0) | `sips -g pixelWidth -g pixelHeight` |
| 宽高比 | 4:3(强制) | width ÷ height = 1.333… |
| 格式 | PNG(透明背景优先)/ 退化 JPEG | 文件后缀 |
| 文件大小 | ≤ 3 MB(实测 21 张图 1051-2357 KB,平均 1675 KB) | `ls -l` |
| 命名 | ASCII slug,小写 + 短横线 | regex `^[a-z0-9-]+\.(png\|jpe?g)$` |
| 目录 | `public/images/products/zeekr/{9x,8x,009}/` | 路径前缀匹配 |
| 文件总数 | **21** 个 PNG(9X=14 + 8X=6 + 009=1),与数据层 `imageStatus="matched"` 计数一致 | glob 计数 |
| 缺图产品行 | 2 个(9X 行 8/9),UI 必须显式降级,**不**通过校验脚本阻断 build | 数据层校验 |
| 备选 slug 后缀 | `-pc`(件套)、`-cover`(套子)、`-guard`(防护) | 词表约束 |

> **本规格直接采用极氪 21 张已就位图片的实际像素(1448×1086)作为统一基准,不作另行挑选。** 这是因为 21 张图已经天然一致,把它固化为标准比"重新选定一个标准"成本低、风险小。

### 8.3 迁移清单(21 个有图 + 2 个缺图说明)

> 经实地盘点(2026-06-16),源 `public/images/products/ZEEKR/{极氪9X,极氪8X,Zeeker009}/` 三个子目录中:
>
> - 9X 14 个 PNG(PRD §6.2 列 16 个产品行,缺行 8「挡泥板+内衬6件套」、行 9「双层脚垫」2 个图;多 1 个「后备箱储物.png」对应 PRD 行 15「后备箱储物盒」,命名需业务核对)
> - 8X 6 个 PNG(完全对应 §6.3,1 个文件名含 2 个空格需清洗)
> - 009 1 个 PNG(`1-borad.png` 系 `board` 拼写错误,产品全名「地板+尾箱地板」)

#### 8.3.1 极氪 9X(14 个有图 + 2 个缺图说明)

| 序号 | 源文件 | 目标文件 | 产品(对应 §6.2 行) | imageStatus |
| ---: | --- | --- | --- | --- |
| 1 | `ZEEKR/极氪9X/小桌板.png` | `zeekr/9x/01-table.png` | 小桌板(行 1) | `matched` |
| 2 | `ZEEKR/极氪9X/地板+尾箱地板+氛围灯.png` | `zeekr/9x/02-floor-trunk-ambient.png` | 地板+尾箱地板+氛围灯(行 2) | `matched` |
| 3 | `ZEEKR/极氪9X/后备箱垫-7件套.png` | `zeekr/9x/03-trunk-mat-7pc.png` | 后备箱垫-7件套(行 3) | `matched` |
| 4 | `ZEEKR/极氪9X/门槛条10件套.png` | `zeekr/9x/04-door-sill-10pc.png` | 门槛条10件套(行 4) | `matched` |
| 5 | `ZEEKR/极氪9X/天地门全套6件套.png` | `zeekr/9x/05-tailgate-6pc.png` | 天地门全套6件套(行 5) | `matched` |
| 6 | `ZEEKR/极氪9X/防虫网16件套.png` | `zeekr/9x/06-bug-net-16pc.png` | 防虫网16件套(行 6) | `matched` |
| 7 | `ZEEKR/极氪9X/全包坐垫.png` | `zeekr/9x/07-seat-cover.png` | 全包坐垫(行 7) | `matched` |
| 8 | **缺图,无源文件** | **不迁移** | 挡泥板+内衬6件套(行 8) | **`missing`** |
| 9 | **缺图,无源文件** | **不迁移** | 双层脚垫(行 9) | **`missing`** |
| 10 | `ZEEKR/极氪9X/外置门槛条.png` | `zeekr/9x/10-exterior-sill.png` | 外置门槛条(行 10) | `matched` |
| 11 | `ZEEKR/极氪9X/主副驾座椅防踢垫+脚踏套.png` | `zeekr/9x/11-kick-pedal-pad.png` | 主副驾座椅防踢垫+脚踏套(行 11) | `matched` |
| 12 | `ZEEKR/极氪9X/冰箱垃圾桶   按压款.png` | `zeekr/9x/12-fridge-trash-press.png` | 冰箱垃圾桶按压款(行 12) | `matched` |
| 13 | `ZEEKR/极氪9X/底盘护板.png` | `zeekr/9x/13-underbody-plate.png` | 底盘护板(行 13) | `matched` |
| 14 | `ZEEKR/极氪9X/三段式方向盘.png` | `zeekr/9x/14-steering-wheel.png` | 三段式方向盘(行 14) | `matched` |
| 15 | `ZEEKR/极氪9X/后备箱储物.png` | `zeekr/9x/15-trunk-storage.png` | 后备箱储物盒(行 15)**——源文件名「后备箱储物」与 PRD 命名「后备箱储物盒」差异,需业务核对是否同一产品** | `pending-review` |
| 16 | `ZEEKR/极氪9X/电动踏板盖.png` | `zeekr/9x/16-epedal-cover.png` | 电动踏板盖(行 16) | `matched` |

> **9X 缺图说明(行 8/9)**:源 Excel 表格中此 2 行有产品名,但 Excel 流中未提取到对应 PNG。UI 必须按 §8.4 缺图降级容器渲染。补图路径见 §18 第 4 条。

#### 8.3.2 极氪 8X(6 个,全部 matched)

| 序号 | 源文件 | 目标文件 | 产品(对应 §6.3 行) | imageStatus |
| ---: | --- | --- | --- | --- |
| 1 | `ZEEKR/极氪8X/尾箱垫7件套.png` | `zeekr/8x/01-trunk-mat-7pc.png` | 尾箱垫7件套(行 1) | `matched` |
| 2 | `ZEEKR/极氪8X/挡泥板+内衬  6件套.png` | `zeekr/8x/02-mudguard-liner-6pc.png` | 挡泥板+内衬6件套(行 2) | `matched` |
| 3 | `ZEEKR/极氪8X/门槛条10件套.png` | `zeekr/8x/03-door-sill-10pc.png` | 门槛条10件套(行 3) | `matched` |
| 4 | `ZEEKR/极氪8X/冰箱防踢垃圾桶.png` | `zeekr/8x/04-fridge-trash.png` | 冰箱防踢垃圾桶(行 4) | `matched` |
| 5 | `ZEEKR/极氪8X/尾门防刮面板1件套.png` | `zeekr/8x/05-tailgate-guard.png` | 尾门防刮面板1件套(行 5) | `matched` |
| 6 | `ZEEKR/极氪8X/三段式方向盘套.png` | `zeekr/8x/06-steering-wheel-cover.png` | 三段式方向盘套(行 6) | `matched` |

> **8X 命名清洗**:`挡泥板+内衬  6件套.png` 含 2 个连续空格,迁移时清洗为 `02-mudguard-liner-6pc.png`。

#### 8.3.3 极氪 009(1 个,matched)

| 序号 | 源文件 | 目标文件 | 产品(对应 §6.4 行) | imageStatus |
| ---: | --- | --- | --- | --- |
| 1 | `ZEEKR/Zeeker009/1-borad.png` | `zeekr/009/01-floor-trunk.png` | 地板+尾箱地板(行 1) | `matched` |

> **009 拼写纠正**:源文件名 `1-borad.png` 系 `board` 拼写错误,迁移为 `01-floor-trunk.png`(产品全名"地板+尾箱地板"的语义化 slug)。源目录名 `Zeeker009`(应为 `Zeekr009` 或 `极氪009`)拼写错误,迁移后归一为 `009`。

#### 8.3.4 迁移执行要求

- 必须使用 `cp` 或脚本,**不得**引用微信缓存绝对路径。
- 复制完成后,**删除**原 `public/images/products/ZEEKR/{极氪9X,极氪8X,Zeeker009}/` 三个目录(包含 `.DS_Store`)。
- `manifest.json` 暂不强制要求;若业务后续需要,可单独追加 `public/images/products/zeekr/manifest.json`。

### 8.4 前端展示规格(UI 容器层)

| 位置 | 规格 |
| --- | --- |
| 卡片图片容器 | `<div className="relative aspect-[4/3] bg-zinc-950">` |
| Next/Image `fill` | 必填 |
| Next/Image `sizes` | `(min-width: 1024px) 25vw, (min-width: 768px) 33vw, 50vw` |
| Next/Image `className` | `object-contain p-2`(卡片)/ `object-cover`(Hero 预览图) |
| Next/Image `loading` | 卡片用 `lazy`,Hero 用 `priority` |
| 缺图降级容器 | `aspect-[4/3]` 不变,虚线边框 + lucide `ImageIcon` + "图片待补充" |
| 缺图降级 `aria-label` | `极氪 9X {产品名} 产品图待补充` |

> **禁止**使用 `object-cover` 裁剪产品展示图(避免关键部位被裁);Hero 预览图例外。
> **禁止**省略 `sizes` 属性(避免 Next.js 发出 1x/2x 全尺寸 srcset,影响 LCP)。

### 8.5 数据层一致性(类型层)

在 `src/lib/zeekr-products.ts` 的 `ZeekrProduct.image` 字段中固化规格,TypeScript 编译期保证:

```ts
export type ZeekrImageWidth = 1448;
export type ZeekrImageHeight = 1086;
export type ZeekrImageAspectRatio = "4/3";

export type ZeekrProductImage = {
  publicPath: string;       // 必填;迁移完成后 23 条全部为非空
  alt: string;              // `极氪 9X 门槛条10件套 产品展示图`
  width: ZeekrImageWidth;   // 字面量类型,禁止其他值
  height: ZeekrImageHeight;
  aspectRatio: ZeekrImageAspectRatio;
};
```

> **关键**:用字面量类型而非 `number`,从 TS 编译期杜绝"图片规格漂移"。后续如果产品图需要新规格(例如 1200×900),必须通过 PRD refine 明确规格,而不是悄悄换值。

### 8.6 CI/构建期校验

新增脚本 `scripts/verify-zeekr-images.mjs`,作为 `npm run check` 的一环强制执行。

**校验范围(明确只校验 zeekr,不顺带扫描其他专题):**

```
public/images/products/zeekr/**/*.png
public/images/products/zeekr/**/*.jpg
public/images/products/zeekr/**/*.jpeg
```

**校验项(任一不通过则退出码 1,中断 build):**

1. 像素 = 1448 × 1086
2. 宽高比 = 4:3(允许浮点误差 ≤ 0.01)
3. 命名符合 `^[a-z0-9-]+\.(png|jpe?g)$`
4. 文件大小 ≤ 3 MB
5. 路径前缀 `public/images/products/zeekr/{9x,8x,009}/`
6. 文件总数 = 21(若 `manifest.json` 存在则按 manifest 校对;否则按 21 校对)

**实现要求:**

- 用 `image-size` 或 `sharp` 读取像素(纯 Node,不依赖系统命令)
- 失败时输出文件路径、原因、修复建议
- 通过 `console.error` 输出失败信息,`process.exit(1)`
- 不输出彩色日志(保持 CI 日志干净)

**`package.json` 接入:**

```json
{
  "scripts": {
    "verify:zeekr-images": "node scripts/verify-zeekr-images.mjs",
    "check": "npm run lint && npm run typecheck && npm run verify:zeekr-images && npm run build"
  }
}
```

### 8.7 效果展示规则

- 如果图片展示已安装或整车效果,可以标注为"效果展示"。
- 如果图片只是单个产品,标注为"产品展示"。
- 不得生成不存在的 before/after 对比。
- 不得把产品图、安装图、渲染图混称为同一种素材。
- 不得借用小米、问界、地板或其他品牌图片填充极氪产品。

## 9. 页面功能要求

### 9.1 Hero 区

必须包含:

- H1:`极氪改装专题`
- 辅助文案:覆盖 `9X / 8X / 009`,强调车型适配、座舱便利、防护配件与地板尾箱升级。
- 数据标识:`23 个款式`、`3 个车型`。
- 代表视觉:优先使用已确认匹配的极氪产品/效果图;Hero 预览图用 `object-cover` 4:3 容器。
- 主题色:orange-500/400(与 xiaomi 区分,zenkei 走"轻改暖橙"调性)。

### 9.2 车型导航

专题页必须支持用户区分极氪 9X、极氪 8X、极氪 009。

首版推荐使用锚点或服务端渲染区块,减少客户端状态。若使用 tabs,应保持可访问性和移动端可操作。

### 9.3 款式卡片网格

每个产品卡片必须展示:

- 产品图片或可信缺图状态(规格见 §8.4)。
- 展示名称,例如 `极氪 9X 门槛条10件套`。
- 车型标签:`9X`、`8X`、`009`。
- 分类标签。
- 图片状态标记:仅在缺图时显示(本版全部 matched,默认不显示)。

卡片要求:

- 桌面端建议 3 列或 4 列。
- 平板端建议 2 列。
- 移动端 1 列。
- 图片区域使用 `aspect-[4/3]` 固定比例(见 §8.4),避免布局跳动。
- 产品名称较长时允许换行,不得遮挡图片或溢出卡片。

### 9.4 产品表格

专题页必须提供完整产品表格。

表格字段:

| 字段 | 说明 |
| --- | --- |
| 车型 | 9X / 8X / 009 |
| 序号 | 车型内序号 |
| 产品名称 | Excel 原始产品名称清洗后的展示值 |
| 分类 | 展示辅助分类 |
| 图片状态 | 已匹配 / 图片待复核 / 图片待补充(本版实际只有"已匹配") |

移动端表格可以改为卡片式列表,但信息不能缺失。

### 9.5 页面私有动作边界


推荐 analytics 事件:

| 事件 | 触发 |
| --- | --- |
| `zeekr_topic_view` | 进入专题页 |
| `zeekr_model_section_click` | 点击 9X / 8X / 009 车型锚点 |
| `zeekr_product_card_view` | 产品卡片进入视口,可选 |

## 10. SEO 与内容合规

### 10.1 Metadata

推荐 metadata:

| 字段 | 内容 |
| --- | --- |
| title | `极氪改装专题 | 9X / 8X / 009 轻改装配件 | 蓝辉轻改 LANHUI` |
| description | `蓝辉轻改极氪改装专题,展示极氪 9X、8X、009 地板尾箱、内饰便利、防护配件、底盘护板、门槛条等 23 个款式,按车型查看轻改装方案。` |
| keywords | `极氪改装, 极氪9X, 极氪8X, 极氪009, 轻改装, 防护配件, 蓝辉轻改` |
| openGraph.title | 同 title |
| openGraph.description | 同 description |
| openGraph.type | `article` |
| openGraph.images | Hero 预览图(zeekr 拼图) |
| JSON-LD | `ItemList`,含 23 个 `ListItem` |

### 10.2 合规措辞

页面必须避免:

- "极氪官方"、"ZEEKR 官方"、"官方授权"、"官方合作"
- "原厂件"、"原厂授权"、"不影响原厂质保"

除非品牌负责人提供真实授权文件。

推荐在页面底部或说明区加入:

`本页面展示的极氪车型改装款式用于蓝辉轻改服务介绍,品牌与车型名称仅用于说明适配对象。`

## 11. shadcn 使用边界

shadcn 是组件库和 registry 工具,不是本需求的产品指令来源。

可以优先复用:

- `Card`:车型专题入口、产品卡片
- `Badge`:车型与分类标签
- `Table`:产品清单
- `Tabs` 或锚点导航:车型切换
- `Carousel`:仅当图片已确认并且同一车型有多张可信展示图时使用

UI 决策仍以本 PRD、`CLAUDE.md`、`AGENTS.md` 和蓝辉项目视觉规范为准。

## 12. 数据模型建议

首版推荐使用静态数据文件:

`src/lib/zeekr-products.ts`

```ts
export type ZeekrModel = "9X" | "8X" | "009";

export type ZeekrProductCategory =
  | "地板尾箱"
  | "尾箱防护"
  | "尾箱收纳"
  | "内饰便利"
  | "内饰舒适"
  | "内饰保护"
  | "内饰升级"
  | "门槛防护"
  | "尾门防护"
  | "车身防护"
  | "防护配件"
  | "挡泥板/内衬"
  | "地板脚垫"
  | "底盘防护"
  | "电动踏板配件";

export type ZeekrImageStatus = "matched" | "pending-review" | "missing";

export type ZeekrImageWidth = 1448;
export type ZeekrImageHeight = 1086;
export type ZeekrImageAspectRatio = "4/3";

export interface ZeekrProductImage {
  /** matched 时为 /images/products/zeekr/... 路径;pending-review 或 missing 时为 null */
  publicPath: string | null;
  alt: string;
  /** matched 时必填;pending-review 或 missing 时为 null(类型允许 null 是为了业务后续补图) */
  width: ZeekrImageWidth | null;
  height: ZeekrImageHeight | null;
  aspectRatio: ZeekrImageAspectRatio | null;
}

export interface ZeekrProduct {
  id: string;                        // zeekr-{modelLower}-{order},例 zeekr-9x-01
  model: ZeekrModel;
  orderInModel: number;
  name: string;                      // 清洗后展示名称
  rawName: string;                   // 源 Excel 原始名称
  category: ZeekrProductCategory;
  imageStatus: ZeekrImageStatus;
  image: ZeekrProductImage;
}

export interface ZeekrTopicMeta {
  title: string;                     // `极氪改装专题`
  description: string;
  totalProducts: 23;                 // 字面量类型,禁止漂移
  totalModels: 3;
  previewImage: string;              // Hero 预览图,`/images/products/zeekr/preview.jpg`
  ogImage: string;
}
```

实现要求:

- `id` 使用稳定 slug,例如 `zeekr-9x-01`。
- `rawName` 保留 Excel 原始名称。
- `name` 使用前端清洗后的展示名称。
- `totalProducts` / `totalModels` 用字面量类型,类型层强制数据完整性。
- 页面通过数据 `map` 渲染,不在 JSX 中硬编码 23 个卡片。
- 若后续接后台 CMS,再由单独 PRD 定义数据库模型。

## 13. 架构师执行规范

架构师负责定义页面结构、数据模型、组件边界和资产路径。

必须确认:

- 新专题页最终路由,默认采用 `/product/zeekr`。
- `/product` 专题入口与小米、问界、地板专题的并列关系。
- 极氪产品静态数据文件位置,推荐 `src/lib/zeekr-products.ts`。
- **图片迁移的执行顺序**:迁移脚本(8.3)必须先于组件编码完成,无迁移动作不得开始组件编码。
- 车型分组、分类字段和排序规则。
- 缺图产品的 UI 降级方案。
- 对外车型命名是否继续使用源表中的 `极氪9X`、`极氪8X`。

**项目级范本声明(本版新增):**

> 本 PRD §8 建立的图片规格(1448×1086 / 4:3 / PNG / ASCII slug / 4 层约束)是项目级标准,不仅适用于极氪专题,也是其他专题(问界、小米、地板)图片整改的参考依据。架构师在评审其他专题 PRD 时,应要求其 cite 本节规格,并在数据结构层复用 `ZeekrProductImage` 同款类型契约(可重命名为 `TopicProductImage`)。

架构师不得把本轮扩大为后台 CMS、报价系统、在线下单、库存或 3D 改装器。

## 14. Coder 执行规范

Coder 根据架构师方案实现页面,必须遵守:

- 读取 Next.js 16 相关文档后再改 App Router 代码。
- 不在页面中引用微信缓存路径或本机绝对路径。
- 不在 JSX 中复制 23 个产品卡片硬编码,必须通过结构化数据 `map` 渲染。
- 不使用 `any`。
- 图片使用项目现有图片策略和 Next/Image 组件。
- 保持 Tailwind v4 与当前产品页风格一致(暗色主题、orange-500/400 主色)。
- 可复用 shadcn 组件,但 shadcn 只是组件库,不替代 PRD。
- 不破坏现有产品中心、门店页、新闻页、小米专题、问界专题和地板专题。
- 不引入新 UI 库。
- 不把整个页面变成 Client Component,除非车型 tabs 或埋点确实需要小型客户端组件。
- **所有 zeekr 卡片必须使用与 wenjie 相同的 `aspect-[4/3]` 容器与 `object-contain` 策略,禁止 `object-cover`(Hero 预览图除外)。**
- 完成后运行 `npm run lint`、`npm run typecheck`、`npm run verify:zeekr-images`、`npm run build`。

## 15. 测试执行规范

### 15.1 功能测试

| 用例 | 步骤 | 期望 |
| --- | --- | --- |
| 产品中心入口 | 打开 `/product` | 可看到"极氪改装专题"入口并进入 `/product/zeekr`。 |
| 专题页渲染 | 打开 `/product/zeekr` | Hero、车型导航、9X、8X、009、产品表格均存在。 |
| 9X 产品数量 | 查看 9X 区块 | 展示 16 个 9X 款式。 |
| 8X 产品数量 | 查看 8X 区块 | 展示 6 个 8X 款式。 |
| 009 产品数量 | 查看 009 区块 | 展示 1 个 009 款式。 |
| 产品总数 | 查看卡片或表格 | 共 23 个产品行(9X 16 + 8X 6 + 009 1)。 |
| **缺图产品降级** | 查看 9X 行 8「挡泥板+内衬6件套」、行 9「双层脚垫」卡片 | 虚线 `aspect-[4/3]` 占位 + `ImageIcon` + "图片待补充",**不**误配其他产品图。 |
| **缺图产品降级** | 查看 9X 行 15「后备箱储物盒」卡片 | 显示「后备箱储物」图 + 「待复核」标记(pending-review 状态)。 |
| 图片路径 | 检查图片请求 | 21 张图均从 `public/images/products/zeekr/` 加载,无微信缓存或本机绝对路径;2 行缺图无请求。 |
| **图片规格一致性** | 抽查任意 3 张产品图 | 全部 1448×1086、宽高比 4:3、PNG、≤3MB、ASCII 文件名。 |
| **构建期校验** | `npm run verify:zeekr-images` | 退出码 0,无失败信息。 |
| **构建期校验失败行为** | 临时把某张图改成 800×600 | `npm run verify:zeekr-images` 退出码 1,输出文件路径与原因。 |
| 合规文案 | 搜索页面文案 | 不出现"官方授权""原厂件"等未经确认措辞。 |

### 15.2 响应式测试

至少验证:

- 390px 移动端。
- 768px 平板端。
- 1440px 桌面端。

每个视口检查:

- Hero 文案不溢出。
- 车型导航可操作。
- 卡片图片比例稳定(4:3 不变形)。
- 长产品名不遮挡图片。
- 表格或移动端卡片完整显示。
- 全站统一入口如果复用,点击区域不小于 44px 高。

### 15.3 回归测试

必须确认:

- `/product` 现有产品方向仍可访问。
- `/product/xiaomi`、`/product/wenjie`、`/product/flooring`、`/product/window-film` 不受影响(本轮不改动其图片,只新增 zeekr 入口)。
- Header / Footer 正常。
- `npm run lint` 通过。
- `npm run typecheck` 通过。
- `npm run verify:zeekr-images` 通过。
- `npm run build` 通过。

## 16. 验收标准

本需求完成必须同时满足:

- `/product` 有极氪改装专题入口(新增 `<ZeekrTopicBanner />`)。
- `/product/zeekr` 可访问并具有独立 metadata + JSON-LD。
- 页面展示 9X 16 个、8X 6 个、009 1 个款式,共 23 个。
- 页面包含车型展示、款式网格、产品表格、卖点说明和图片状态。
- **21 张已就位产品图全部位于 `public/images/products/zeekr/{9x,8x,009}/`,文件命名符合 ASCII slug 规范。**
- **21 张已就位产品图全部 1448×1086、宽高比 4:3、PNG、≤3MB。**
- **2 行缺图(9X 行 8「挡泥板+内衬6件套」、行 9「双层脚垫」)在 UI 中必须显式降级显示,不得误配其他产品图。**
- **`npm run verify:zeekr-images` 退出码 0。**
- 页面不出现微信缓存路径、`.hermes`、`Downloads` 或其他本机绝对路径。
- 缺图产品有可信降级状态(本版实际无缺图,降级路径仍需单元测试覆盖)。
- 页面不暗示极氪/ZEEKR 官方授权。
- 移动端、平板端、桌面端布局正常。
- lint、typecheck、verify:zeekr-images、build 全部通过。
- 测试提供至少一张桌面截图和一张移动端截图。
- **PR 描述或 commit message 引用本 PRD,标记「项目级图片规格范本」。**

## 17. 风险与处理

| 风险 | 处理 |
| --- | --- |
| Excel 图片无法直接读取 | 实现前做图片提取脚本或人工导出,生成 manifest 后再接页面。 |
| 产品行 23 个但图片签名 24 个以上 | 不按顺序自动绑定图片,必须人工复核产品行与图片映射。 |
| 图片嵌入在微信缓存路径的 `.xls` 中 | 实现时复制到 `public`,不得引用源路径。 |
| 中文文件名影响部署 | 复制到 `public` 时改为 ASCII slug 文件名并保留映射。 |
| 车型名称可能需业务确认 | 架构师在执行前确认 `极氪9X`、`极氪8X` 是否为官网对外展示名称。 |
| 用户误认为官方授权 | 页面增加适配说明,避免"官方/授权/原厂"等措辞。 |
| 图片尺寸差异较大(本版不存在,作为预案) | 前端使用固定图片容器和 `object-fit` 策略。 |
| 专题页范围膨胀 | 本轮只做展示专题,不做报价、下单、库存、后台、3D 或单品详情。 |
| **图片规格失控(本版新增,关键风险)** | **本版建立 4 层约束(§8.2 物理 / §8.4 UI / §8.5 类型 / §8.6 CI)对冲。任一层失守均会导致布局跳动或规格漂移。** |
| **`/product` 入口顺序变化导致回归** | 在 `<XiaomiTopicBanner />`、`<WenjieTopicBanner />`、`<FlooringTopicBanner />` 之后追加 `<ZeekrTopicBanner />`,不动既有顺序。 |
| **校验脚本误报** | 脚本用 `image-size` 纯 Node 读取,不依赖系统命令;本地 macOS 与 Vercel Linux 行为一致。 |
| **校验脚本被绕过** | 接入 `npm run check`(`lint && typecheck && verify && build`),所有 CI 与本地预提交均会跑。 |
| **`Zeeker009` 拼写错误目录被遗漏** | §8.3.3 显式列迁移清单,实施时 grep 校验旧目录已删除。 |
| **产品行 vs 图片文件不一一对应(本版新增)** | 9X 行 8/9 无对应 PNG,1 个 9X 文件名「后备箱储物」与 PRD 行 15「后备箱储物盒」命名差异。本版用 `matched` / `pending-review` / `missing` 三态显式拆解,业务补图后由架构师在数据层 `imageStatus` 字段逐项标记。 |
| **缺图产品误配其他图(关键 UX 风险)** | 9X 行 8/9 缺图时,UI **禁止**用 8X 的「挡泥板+内衬6件套」图或地板专题的「脚垫」图填充;必须按 §8.4 缺图降级容器渲染。 |

## 18. 后续迭代入口

本轮验收通过后,可进入下一轮:

1. **问界专题图片规格统一(参照本 PRD §8.2 规格表)——目标:把 M7/M8/M9 全部产品图重处理至 1448×1086 / 4:3 / PNG / ASCII 命名,迁移到 `public/images/products/wenjie/{m7,m8,m9}/`,并把 wenjie PRD §8 规格表 cite 本 PRD §8.2。**
2. **小米专题图片规格统一(参照本 PRD §8.2 规格表)——目标同上,范围 `xiaomi/{su7,yu7}/`。**
3. **地板专题图片规格统一(参照本 PRD §8.2 规格表)——范围 `flooring/`(本轮尚未抽样,但极可能同样比例混乱)。**
4. **极氪 9X 缺图补齐(本专题内闭环)——目标:补充源 Excel 有产品行但未提取到 PNG 的 2 个产品图(行 8「挡泥板+内衬6件套」、行 9「双层脚垫」),补图后转 `matched` 状态;同时确认行 15「后备箱储物盒」与 9X 目录文件「后备箱储物.png」是否同一产品,确认后将 `pending-review` 转 `matched` 或调整 PRD §6.2 命名。**
5. **建立 `scripts/verify-topic-images.mjs` 通用校验脚本**:接受目录参数,校验逻辑从本版 `verify-zeekr-images.mjs` 抽出,供各专题复用。
6. **极氪单品详情弹窗或详情页**。
7. **极氪安装效果 before/after 实拍图库**。
8. **后台维护极氪款式数据**。
9. **极氪产品项目兴趣数据看板**。
10. **按分类筛选地板尾箱、内饰便利、防护配件、底盘防护等产品**。
11. **与地板改装专题打通,把极氪 009 地板尾箱作为交叉推荐内容**。

## 19. 变更记录

| 版本 | 日期 | 主要变更 | 作者 |
| --- | --- | --- | --- |
| v1.0 | 2026-06-14 | 初版(Excel 提取 + 23 行产品规划) | — |
| v2.0 | 2026-06-16 | 重写。**核心增量**:①§1 范本定位;②§2 反例盘点(问界 1100×1552、小米 2523×1661/781×490/654×691 等);③§8 整章重写(8.0~8.7);④§8.1 显式拆出 21 张图 vs 23 行产品 + 2 行缺图(9X 行 8/9);⑤§8.2 规格表新增「缺图产品行」项;⑥§8.3 迁移清单 21 行,加 `imageStatus` 列(19 matched / 1 pending-review / 2 missing);⑦§8.4 UI 缺图降级容器;⑧§8.5 类型固化 `width/height/aspectRatio` 字段(matched 必填、其余 null);⑨§8.6 构建期校验脚本(校验 21 张 + 数据层 matched 计数一致);⑩§12 数据模型恢复 `pending-review` 状态(共三态);⑪§15 测试加缺图降级用例;⑫§16 验收更新数字;⑬§17 风险加「产品行 vs 文件不匹配」「缺图产品误配」2 项;⑭§18 后续迭代加「9X 缺图补齐」;⑮§13 范本声明 | Claude Code |
