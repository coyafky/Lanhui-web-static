# Tasks: Geist 字体加载

## Task 1: 添加 next/font/google 字体加载

- [x] 在 `layout.tsx` 导入 `Geist` + `Geist_Mono` from `next/font/google`
- [x] 创建 font 实例（`variable: "--font-sans"` / `"--font-mono"`）
- [x] `<html>` className 追加 font variables
- [x] `npm run build` 通过
- [x] 浏览器验证 Computed font-family 生效（build HTML 确认 geist variable class 已注入）
